// b)

test("Creating Mocks", () => {
  const validatorMock = jest.fn(() => {
    return true;
  });
});

// c)

test("Creating Mocks", () => {
  const validatorMock = jest.fn(() => {
    return false;
  });
});
